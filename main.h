#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <stdlib.h>

double getAverage(int arr[], int size);
int getMedian(int arr[], int size);
void total_student(char name, int adm, int class);

#endif
